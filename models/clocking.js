class Clocking {
  constructor(clockingType, reasonId, beaconId, lat, lng, address) {
    ctype: this.clockingYype;
    reasonid: this.reasonId;
    beaconid: this.beaconId;
    lat: this.lat;
    lon: this.lng;
    clocation: this.address
  }
};

export default Clocking;